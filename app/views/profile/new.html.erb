<%= form_for [@user, @profile] do |f| %>
  <%= f.select :provider, [['Github'], ['LinkedIn'], ['Twitter'], ['Facebook'], ['Pinterest'], ['Instagram']] %>
  <%= f.url_field :link %>
  <%= f.submit "Add Profile" %>
<% end %>
